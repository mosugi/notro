---
/**
 * Explicit empty line spacer.
 * Notion strips plain empty lines, so a dedicated element is used.
 */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["my-2", className]} aria-hidden="true"></div>
