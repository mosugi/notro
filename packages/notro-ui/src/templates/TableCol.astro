---
import { tv } from 'tailwind-variants';

export const tableCol = tv({ base: '' });

interface Props { class?: string; }
const { class: className } = Astro.props;
---
<col class={tableCol({ class: className })} />
