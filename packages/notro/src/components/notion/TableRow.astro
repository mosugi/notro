---
import { colorToCSS } from './colors';
import { getClass } from '../../utils/classRegistry';
interface Props { color?: string; class?: string; }
const { color, class: className } = Astro.props;
---
<tr class:list={["notion-tr", getClass('tableRow'), className]} style={colorToCSS(color)}><slot /></tr>