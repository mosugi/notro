---
import type { NotionColor } from "./colors.ts";
interface Props { color?: NotionColor; class?: string; }
const { color, class: className } = Astro.props;
---
<tr data-color={color || undefined} class:list={[className]}><slot /></tr>
