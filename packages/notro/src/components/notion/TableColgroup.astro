---
/**
 * Mapped to MDX `components.colgroup`.
 * Accepts an optional class prop (e.g. injected via classMap.colgroup).
 */
interface Props {
	class?: string;
}
const { class: className } = Astro.props;
---
<colgroup class:list={[className]}><slot /></colgroup>
