---
import { tv } from 'tailwind-variants';

export const tableColgroup = tv({ base: '' });

interface Props { class?: string; }
const { class: className } = Astro.props;
---
<colgroup class={tableColgroup({ class: className })}><slot /></colgroup>
