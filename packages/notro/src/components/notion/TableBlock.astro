---
/**
 * <table fit-page-width="true|false" header-row="true|false" header-column="true|false">
 *   ...
 * </table>
 *
 * Mapped to MDX `components.table`.
 * Color priority (high → low): cell > row > column
 */
interface Props {
	'fit-page-width'?: string;
	'header-row'?: string;
	'header-column'?: string;
	class?: string;
	tableWrapperClass?: string;
}

const {
	'fit-page-width': fitPageWidth = 'false',
	'header-row': headerRow = 'false',
	'header-column': headerColumn = 'false',
	class: className,
	tableWrapperClass,
} = Astro.props;
---

<div
	class:list={[
		'nt-table-wrapper',
		tableWrapperClass,
		{ 'nt-table-wrapper--full': fitPageWidth === 'true' },
	]}
>
	<table
		class:list={[
			'nt-table',
			className,
			{ 'nt-table--header-row': headerRow === 'true' },
			{ 'nt-table--header-col': headerColumn === 'true' },
		]}
	>
		<slot />
	</table>
</div>
