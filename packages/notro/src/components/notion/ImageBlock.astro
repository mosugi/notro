---
/**
 * ![Caption](URL) {color="Color"}
 *
 * Notion image block. Mapped to MDX `components.img`.
 */
import type { NotionColor } from "./colors.ts";
interface Props { src: string; alt?: string; caption?: string; color?: NotionColor; class?: string; }
const { src, alt, caption, color, class: className } = Astro.props;
---
<figure data-image-block data-color={color || undefined} class:list={[className]}>
	<img src={src} alt={alt ?? ''} loading="lazy" decoding="async" data-image-img />
	{caption && <figcaption data-image-caption>{caption}</figcaption>}
</figure>
