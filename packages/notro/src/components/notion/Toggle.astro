---
/**
 * <details color="gray_bg">
 *   <summary>Toggle title</summary>
 *   Children
 * </details>
 *
 * Mapped to MDX `components.details`.
 * Used together with ToggleTitle.astro (components.summary).
 */
import type { NotionColor } from "./colors.ts";

interface Props {
	color?: NotionColor;
	class?: string;
}

const { color, class: className } = Astro.props;
---

<details data-color={color || undefined} class:list={[className]}>
	<slot />
</details>
