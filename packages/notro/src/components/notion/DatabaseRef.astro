---
/**
 * <database url="URL" inline="true|false" icon="Emoji" color="Color">Title</database>
 *
 * Link to a Notion database.
 */
import type { NotionColor } from "./colors.ts";
interface Props { url: string; inline?: string; icon?: string; color?: NotionColor; class?: string; }
const { url, inline = 'false', icon = '🗄️', color, class: className } = Astro.props;
---
<a
	href={url}
	data-databaseref
	data-color={color || undefined}
	data-inline={inline === 'true' ? '' : undefined}
	class:list={[className]}
>
	<span data-databaseref-icon aria-hidden="true">{icon}</span>
	<slot />
</a>
