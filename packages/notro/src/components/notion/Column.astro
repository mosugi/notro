---
/**
 * <column>
 *   Children
 * </column>
 *
 * Placed directly inside Columns.astro.
 */
import { getClass } from '../../utils/classRegistry';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["notion-column", getClass('column'), className]}>
	<slot />
</div>
