---
import { colorToCSS } from './colors';
interface Props { color?: string; id?: string; class?: string; }
const { color, id, class: className } = Astro.props;
---
<h3 {id} class:list={["notion-h3", className]} style={colorToCSS(color)}><slot /></h3>