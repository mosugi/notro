---
/**
 * Notion video embed block.
 */
import { tv } from 'tailwind-variants';

export const video = tv({ base: 'my-4' });

interface Props { src: string; class?: string; }
const { src, class: className } = Astro.props;
---

<figure class={video({ class: className })}>
  <video controls src={src} class="w-full"></video>
  {Astro.slots.has('default') && (
    <figcaption class="text-sm text-[var(--notro-text-muted)] mt-1.5">
      <slot />
    </figcaption>
  )}
</figure>
