---
/**
 * <page url="URL" color="Color">Title</page>
 *
 * Link to a Notion page.
 */
import type { NotionColor } from "./colors.ts";
interface Props { url: string; color?: NotionColor; class?: string; }
const { url, color, class: className } = Astro.props;
---
<a href={url} data-pageref data-color={color || undefined} class:list={[className]}>
	<span data-pageref-icon aria-hidden="true">📄</span>
	<slot />
</a>
