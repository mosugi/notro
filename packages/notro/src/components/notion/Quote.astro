---
/**
 * > Rich text {color="Color"}
 *   Children
 *
 * Mapped to MDX `components.blockquote`.
 */
interface Props {
	color?: string;
	class?: string;
}

const { color, class: className } = Astro.props;
---

<blockquote data-color={color || undefined} class:list={[className]}>
	<slot />
</blockquote>
