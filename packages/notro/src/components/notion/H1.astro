---
import type { NotionColor } from "./colors.ts";
interface Props { color?: NotionColor; id?: string; class?: string; }
const { color, id, class: className } = Astro.props;
---
<h1 {id} data-color={color || undefined} class:list={[className]}><slot /></h1>
