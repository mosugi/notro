---
/**
 * <columns>
 *   <column>...</column>
 *   <column>...</column>
 * </columns>
 */
import { getClass } from '../../utils/classRegistry';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["notion-columns", getClass('columns'), className]}>
	<slot />
</div>
