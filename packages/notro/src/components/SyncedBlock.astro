---
import type { SyncedBlockBlockObjectResponseType } from "../loader/schema";
interface Props {
  block: SyncedBlockBlockObjectResponseType;
  id?: string;
}

const { block, id } = Astro.props;
---

<div {id} class=`nt-selectable nt-transclusion_reference-block`>
  <slot />
</div>
