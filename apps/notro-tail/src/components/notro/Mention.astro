---
/**
 * Inline mention (user / page / database / data source / agent).
 */
interface Props {
  url: string;
  class?: string;
}

const { url, class: className } = Astro.props;
---

<a href={url} class:list={["underline underline-offset-2 hover:opacity-70", className]}>@<slot /></a>
