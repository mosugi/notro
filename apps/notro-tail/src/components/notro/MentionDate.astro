---
/**
 * Inline date mention.
 */
interface Props {
  start: string;
  end?: string;
  startTime?: string;
  timeZone?: string;
  class?: string;
}

const { start, end, startTime, timeZone, class: className } = Astro.props;

const parts: string[] = [start];
if (startTime) parts.push(startTime);
if (end) parts.push(`→ ${end}`);
if (timeZone) parts.push(`(${timeZone})`);
const display = parts.join(' ');
---

<time class:list={["font-mono text-sm", className]} datetime={start}>{display}</time>
